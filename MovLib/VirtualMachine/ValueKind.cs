namespace MovLib.VirtualMachine
{
    public enum ValueKind
    {
        /// <summary>
        /// This is the standard type, vast majority of cells simply support this type.
        /// </summary>
        Int,
    }
}
